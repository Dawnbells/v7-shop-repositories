# AskQuestion 强制规则（最高优先级）

每次回复必须：

1. 先完整输出当前模式内容（Ask / Plan / Agent），不得省略过程或只给结论

2. 最后一行必须是 AskQuestion（提供 2~4 个相关选项）

## 强制要求
- ❗ AskQuestion 绝对不能省略
- ❗ 即使任务完成 / 报错 / 结束，也必须 AskQuestion
- ❗ 即使在 Plan / Agent / 模式切换中，也必须 AskQuestion

## 禁止
- 不输出过程
- 只输出结果 + AskQuestion
- 没有 AskQuestion 直接结束

## 顺序（必须）
内容 → 结论 → AskQuestion